spring:
  application:
    name: web
#  cloud:
#    config:
#      uri: https://localhost:8888
#  profiles:
#    active: development

server:
  port: 8080

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
feign:
  client:
    config:
      default:
        connectTimeout: 1000
        readTimeout: 2000


